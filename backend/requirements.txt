fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9
sqlalchemy>=2.0.28
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1
PyMuPDF>=1.23.26
pdfplumber>=0.11.0
Pillow>=10.2.0
pandas>=2.2.1
openpyxl>=3.1.2
python-docx>=1.1.0
requests>=2.31.0
numpy>=1.26.4
scikit-learn>=1.4.1.post1
matplotlib>=3.8.3
reportlab>=4.1.0
pytest>=8.1.1
httpx>=0.27.0
python-dotenv>=1.0.1
# Optional but recommended: enables real local (network-free) embeddings
# instead of depending on Hugging Face's free feature-extraction API, which
# only serves a small curated list of models and can 400 on valid model
# names ("not supported by provider hf-inference"). Pulls in torch
# (~1-2GB) - safe to skip on constrained hosting; the app falls back
# gracefully to the HF API, then to a local hash-based vector, either way.
# sentence-transformers>=3.0.0
